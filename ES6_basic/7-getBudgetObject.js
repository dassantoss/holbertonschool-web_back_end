export default function getBudgetObject(income, gdp, capita) {
  // Utilizamos la sintaxis de atajo de valores de propiedad de objetos aquí.
  const budget = {
    income,
    gdp,
    capita,
  };

  return budget;
}
